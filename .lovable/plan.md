## מה יקרה

בקובץ יחיד — `src/components/mentor/MentorSalesPage.tsx` — נחליף את כל אובייקט `COPY.he` (הצד העברי בלבד) בגרסה הקודמת שלו מקומיט `97b759e` מ-7 ביולי.

## מה יישאר בדיוק כמו שהוא

- `COPY.en` — כל התוכן האנגלי החדש (Merged Version) לא ייגע.
- קישור התשלום באנגלית `PAYMENT_URL_EN` נשאר `https://www.paypal.com/ncp/payment/TLJZ4QDGZCDWN`.
- קישור התשלום בעברית `PAYMENT_URL_HE` (משולם) נשאר.
- כל מבנה הקומפוננטה, הסקשנים, האייקונים, ה-Bands, ה-Divider, קרוסלת העדויות, וסקשן "בקרוב" — לא משתנים. **הארגון וה-building blocks נשמרים** — רק הטקסט העברי מוחלף חזרה לתמלול הקודם.

## מה יוחזר בעברית (מ-`97b759e`)

- **Hero:** כותרת "המנטור לקליניקה"; subtitle "ליווי למטפלים לקליניקה מלאה. לרשותך בכל זמן, בכל מקום."; body על "ליווי AI אישי שמוביל אותך שלב אחר שלב…"; CTA "אני רוצה את המנטור".
- **Problem/הפער:** "שנים של הכשרות בפסיכותרפיה. אבל אף אחד לא לימד אותך את זה." + 4 השאלות הקודמות (איך מוצאים מטופלים / איך מסבירים ייחודיות / איך ממלאים את הקליניקה באותנטיות / כמה לתמחר).
- **Personas:** 7 הפרסונות בניסוח הקודם ("יש מי ש…") + outro1/outro2 הקודמים.
- **Interlude:** "אף אחד לא לימד אותך את זה. אז איך יכולת לדעת?" (זהה).
- **Solution:** "מה אם היה מישהו שמחזיק אותך בדיוק שם?" + 4 הפסקאות הקודמות + CTA "זה המנטור לקליניקה."
- **Stages / experience / proof / beta / pricing / contact:** יוחזרו מטקסט 97b759e בשלמותם.

## איך זה יעשה טכנית

1. `git show 97b759e:src/components/mentor/MentorSalesPage.tsx` — לחלץ את בלוק `he: { … }` המלא.
2. ב-`MentorSalesPage.tsx` הנוכחי, להחליף רק את התוכן שבין `he: {` לבין הפסיק שסוגר את האובייקט לפני `en: {` — בבלוק העברי הישן.
3. לוודא ש-typecheck עובר (מפתחות הבלוק העברי הישן חייבים לכסות את אותם מפתחות שהרינדור צורך; אם חסר מפתח כלשהו שנוסף בגרסה החדשה — כמו `trustBar` — נשאיר אותו כפי שהוא ב-`he` הישן אם היה קיים, או נוסיף ערך עברי סביר בקצרה כדי לא לשבור את הרינדור).
4. ריצת בדיקה ויזואלית קצרה של `/mentor` כדי לוודא שכל הסקשנים מרונדרים.

## מה לא נעשה

- לא נשנה את המבנה, הצבעים, הפריסה, האייקונים, או את סדר הסקשנים.
- לא ניגע בקומפוננטת `Mentor.tsx` (עמוד הצ׳אט עצמו).
- לא ניצור טבלאות, פונקציות edge, או migrations.

## סיכון ותוקף

הסיכון היחיד הוא אם ה-`he` הישן חסר מפתח שהתווסף בגרסה החדשה (למשל אם ה-`en` החדש מוסיף `trustBar` והרינדור מנסה `t.trustBar` גם ב-RTL). אם זה קורה — נזהה בטייפצ׳ק או ברינדור, ונוסיף רק את המפתחות החסרים כערכים עבריים קצרים תואמים, בלי לשנות את שאר הטקסט הישן.
